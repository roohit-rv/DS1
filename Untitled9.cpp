#include<stdio.h>
#include<string.h>
int main()
{
	char str[100]={"hi there I just watched 2 episodes of boys and it's a lot"};
	int length=strlen(str);
	printf("the length of the string str is %d",length);
	return 0;
}
